#ifndef ALL_H
#define ALL_H

#include <string>

namespace Test
{
    void RunAll();
    void mark_down();
    void regex_match();
    int symmetry_nums();
    template<typename T>
    std::string numToBits(T n);
    template<typename T>
    bool is_num_symmetry(T num);
    int CurlHttps();
    void ReplaceSpace(char** str,int length);
}

#endif // ALL_H
